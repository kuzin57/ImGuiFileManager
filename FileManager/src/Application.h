#include <iostream>
#include "FileSystemFactory.h"
#include "Window.h"
//#include <qt>
void init();

class Application {
    private:
    // FileSystemFactory system_factory;
    public:
        Application(int, char**);
    // Window create_window_using_director();
        // void init();
};